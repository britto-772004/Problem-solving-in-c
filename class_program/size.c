#include <stdio.h>

int main(){

    int number = 30;
    int numerator ; 
    size_of_number2 = 0;
    while(number >0){
        numerator = number / 10;
        size_of_number2 ++;
        number = numerator;
    }

    printf("d",size_of_number2);
}